{
  "buildCommand": "node -e \"0\"",
  "outputDirectory": "web",
  "agents": {
    "framework": "claude-agent-sdk",
    "dir": "agents",
    "timeout": 600,
    "sandbox": {
      "timeout": 600
    }
  }
}
